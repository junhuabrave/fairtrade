[project]
name = "fairtrade-test-suite"
version = "0.1.0"
description = "Telemetry & market simulator for the fairtrade workspace"
requires-python = ">=3.11"

[tool.poetry.dependencies]
python = ">=3.11"
aiohttp = "^3.8"
testplan = "*"

[tool.poetry.scripts]
market-sim = "scripts.market_simulator:main"
fluent-ship = "scripts.fluent_bit_shipper:main"
